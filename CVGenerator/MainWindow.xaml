<Window x:Class="CVGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:CVGenerator.Localization"
        xmlns:conv="clr-namespace:CVGenerator.Converters"
        Title="{loc:Loc Landing.Title}"
        Height="850" Width="1300"
        FlowDirection="{Binding WindowFlowDirection, Source={x:Static loc:LocalizationService.Instance}}"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8">

    <Window.Resources>
        <conv:IntToVisibilityConverter x:Key="IntToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Border Background="#2C3E50" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📄" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{loc:Loc Landing.Title}" FontSize="17" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="{loc:Loc Landing.Subtitle}" FontSize="11" Foreground="#BDC3C7"
                               Margin="12,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Loc Language.Label}" FontSize="12" Foreground="#BDC3C7"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="LanguageCombo" Width="120" Height="28"
                              VerticalContentAlignment="Center" SelectionChanged="LanguageCombo_SelectionChanged">
                        <ComboBoxItem Tag="en" Content="{loc:Loc Language.English}"/>
                        <ComboBoxItem Tag="fr" Content="{loc:Loc Language.French}"/>
                        <ComboBoxItem Tag="ar" Content="{loc:Loc Language.Arabic}"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="12">
            <TextBlock Text="{loc:Loc Landing.Footer}" HorizontalAlignment="Center" Foreground="#7F8C8D" FontSize="12"/>
        </Border>
    </Grid>
</Window>
